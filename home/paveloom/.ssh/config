# Ask for the password on the `GPG_TTY` that called `ssh`
Match host * exec "gpg-connect-agent updatestartuptty /bye"

Host paveloom.dev
  ControlMaster auto
  ControlPath ~/.ssh/sockets/%C
  ControlPersist 600
