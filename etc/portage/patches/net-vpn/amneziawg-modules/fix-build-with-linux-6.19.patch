# Adapted from https://github.com/amnezia-vpn/amneziawg-linux-kernel-module/issues/138#issuecomment-3876984565

--- a/src/compat/simd/include/linux/simd.h
+++ b/src/compat/simd/include/linux/simd.h
@@ -32,9 +32,6 @@
 #if defined(CONFIG_X86_64)
 	if (*ctx & HAVE_SIMD_IN_USE)
 		kernel_fpu_end();
-#elif defined(CONFIG_KERNEL_MODE_NEON)
-	if (*ctx & HAVE_SIMD_IN_USE)
-		kernel_neon_end();
 #endif
 	*ctx = HAVE_NO_SIMD;
 }
@@ -60,7 +57,7 @@
 #if defined(CONFIG_X86_64)
 	kernel_fpu_begin();
 #elif defined(CONFIG_KERNEL_MODE_NEON)
-	kernel_neon_begin();
+	scoped_ksimd();
 #endif
 	*ctx |= HAVE_SIMD_IN_USE;
 	return true;
--- a/src/cookie.c
+++ b/src/cookie.c
@@ -33,7 +33,7 @@
 			   const u8 pubkey[NOISE_PUBLIC_KEY_LEN],
 			   const u8 label[COOKIE_KEY_LABEL_LEN])
 {
-	struct blake2s_state blake;
+	struct blake2s_ctx blake;
 
 	blake2s_init(&blake, NOISE_SYMMETRIC_KEY_LEN);
 	blake2s_update(&blake, label, COOKIE_KEY_LABEL_LEN);
@@ -77,7 +77,7 @@
 {
 	len = len - sizeof(struct message_macs) +
 	      offsetof(struct message_macs, mac1);
-	blake2s(mac1, message, key, COOKIE_LEN, len, NOISE_SYMMETRIC_KEY_LEN);
+	blake2s(key, NOISE_SYMMETRIC_KEY_LEN, message, len, mac1, COOKIE_LEN);
 }
 
 static void compute_mac2(u8 mac2[COOKIE_LEN], const void *message, size_t len,
@@ -85,13 +85,13 @@
 {
 	len = len - sizeof(struct message_macs) +
 	      offsetof(struct message_macs, mac2);
-	blake2s(mac2, message, cookie, COOKIE_LEN, len, COOKIE_LEN);
+	blake2s(cookie, COOKIE_LEN, message, len, mac2, COOKIE_LEN);
 }
 
 static void make_cookie(u8 cookie[COOKIE_LEN], struct sk_buff *skb,
 			struct cookie_checker *checker)
 {
-	struct blake2s_state state;
+	struct blake2s_ctx state;
 
 	if (wg_birthdate_has_expired(checker->secret_birthdate,
 				     COOKIE_SECRET_MAX_AGE)) {
--- a/src/crypto/include/zinc/blake2s.h
+++ b/src/crypto/include/zinc/blake2s.h
@@ -16,7 +16,7 @@
 	BLAKE2S_KEY_SIZE = 32
 };
 
-struct blake2s_state {
+struct blake2s_ctx {
 	u32 h[8];
 	u32 t[2];
 	u32 f[2];
@@ -25,17 +25,17 @@
 	unsigned int outlen;
 };
 
-void blake2s_init(struct blake2s_state *state, const size_t outlen);
-void blake2s_init_key(struct blake2s_state *state, const size_t outlen,
+void blake2s_init(struct blake2s_ctx *state, const size_t outlen);
+void blake2s_init_key(struct blake2s_ctx *state, const size_t outlen,
 		      const void *key, const size_t keylen);
-void blake2s_update(struct blake2s_state *state, const u8 *in, size_t inlen);
-void blake2s_final(struct blake2s_state *state, u8 *out);
+void blake2s_update(struct blake2s_ctx *state, const u8 *in, size_t inlen);
+void blake2s_final(struct blake2s_ctx *state, u8 *out);
 
-static inline void blake2s(u8 *out, const u8 *in, const u8 *key,
-			   const size_t outlen, const size_t inlen,
-			   const size_t keylen)
+static inline void blake2s(const u8 *key, const size_t keylen,
+	       const u8 *in, const size_t inlen,
+	       u8 *out, const size_t outlen)
 {
-	struct blake2s_state state;
+	struct blake2s_ctx state;
 
 	WARN_ON(IS_ENABLED(DEBUG) && ((!in && inlen > 0) || !out || !outlen ||
 		outlen > BLAKE2S_HASH_SIZE || keylen > BLAKE2S_KEY_SIZE ||
--- a/src/crypto/zinc/blake2s/blake2s.c
+++ b/src/crypto/zinc/blake2s/blake2s.c
@@ -37,19 +37,19 @@
 	{ 10, 2, 8, 4, 7, 6, 1, 5, 15, 11, 9, 14, 3, 12, 13, 0 },
 };
 
-static inline void blake2s_set_lastblock(struct blake2s_state *state)
+static inline void blake2s_set_lastblock(struct blake2s_ctx *state)
 {
 	state->f[0] = -1;
 }
 
-static inline void blake2s_increment_counter(struct blake2s_state *state,
+static inline void blake2s_increment_counter(struct blake2s_ctx *state,
 					     const u32 inc)
 {
 	state->t[0] += inc;
 	state->t[1] += (state->t[0] < inc);
 }
 
-static inline void blake2s_init_param(struct blake2s_state *state,
+static inline void blake2s_init_param(struct blake2s_ctx *state,
 				      const u32 param)
 {
 	int i;
@@ -60,14 +60,14 @@
 	state->h[0] ^= param;
 }
 
-void blake2s_init(struct blake2s_state *state, const size_t outlen)
+void blake2s_init(struct blake2s_ctx *state, const size_t outlen)
 {
 	WARN_ON(IS_ENABLED(DEBUG) && (!outlen || outlen > BLAKE2S_HASH_SIZE));
 	blake2s_init_param(state, 0x01010000 | outlen);
 	state->outlen = outlen;
 }
 
-void blake2s_init_key(struct blake2s_state *state, const size_t outlen,
+void blake2s_init_key(struct blake2s_ctx *state, const size_t outlen,
 		      const void *key, const size_t keylen)
 {
 	u8 block[BLAKE2S_BLOCK_SIZE] = { 0 };
@@ -88,7 +88,7 @@
 static void __init blake2s_fpu_init(void)
 {
 }
-static inline bool blake2s_compress_arch(struct blake2s_state *state,
+static inline bool blake2s_compress_arch(struct blake2s_ctx *state,
 					 const u8 *block, size_t nblocks,
 					 const u32 inc)
 {
@@ -96,7 +96,7 @@
 }
 #endif
 
-static inline void blake2s_compress(struct blake2s_state *state,
+static inline void blake2s_compress(struct blake2s_ctx *state,
 				    const u8 *block, size_t nblocks,
 				    const u32 inc)
 {
@@ -167,7 +167,7 @@
 	}
 }
 
-void blake2s_update(struct blake2s_state *state, const u8 *in, size_t inlen)
+void blake2s_update(struct blake2s_ctx *state, const u8 *in, size_t inlen)
 {
 	const size_t fill = BLAKE2S_BLOCK_SIZE - state->buflen;
 
@@ -191,7 +191,7 @@
 	state->buflen += inlen;
 }
 
-void blake2s_final(struct blake2s_state *state, u8 *out)
+void blake2s_final(struct blake2s_ctx *state, u8 *out)
 {
 	WARN_ON(IS_ENABLED(DEBUG) && !out);
 	blake2s_set_lastblock(state);
@@ -206,7 +206,7 @@
 void blake2s_hmac(u8 *out, const u8 *in, const u8 *key, const size_t outlen,
 		  const size_t inlen, const size_t keylen)
 {
-	struct blake2s_state state;
+	struct blake2s_ctx state;
 	u8 x_key[BLAKE2S_BLOCK_SIZE] __aligned(__alignof__(u32)) = { 0 };
 	u8 i_hash[BLAKE2S_HASH_SIZE] __aligned(__alignof__(u32));
 	int i;
--- a/src/crypto/zinc/blake2s/blake2s-x86_64-glue.c
+++ b/src/crypto/zinc/blake2s/blake2s-x86_64-glue.c
@@ -8,10 +8,10 @@
 #include <asm/processor.h>
 #include <asm/fpu/api.h>
 
-asmlinkage void blake2s_compress_ssse3(struct blake2s_state *state,
+asmlinkage void blake2s_compress_ssse3(struct blake2s_ctx *state,
 				       const u8 *block, const size_t nblocks,
 				       const u32 inc);
-asmlinkage void blake2s_compress_avx512(struct blake2s_state *state,
+asmlinkage void blake2s_compress_avx512(struct blake2s_ctx *state,
 					const u8 *block, const size_t nblocks,
 					const u32 inc);
 
@@ -34,7 +34,7 @@
 #endif
 }
 
-static inline bool blake2s_compress_arch(struct blake2s_state *state,
+static inline bool blake2s_compress_arch(struct blake2s_ctx *state,
 					 const u8 *block, size_t nblocks,
 					 const u32 inc)
 {
--- a/src/crypto/zinc/selftest/blake2s.c
+++ b/src/crypto/zinc/selftest/blake2s.c
@@ -2072,7 +2072,7 @@
 		buf[i] = (u8)i;
 
 	for (i = 0; i < ARRAY_SIZE(blake2s_keyed_testvecs); ++i) {
-		blake2s(hash, buf, key, BLAKE2S_HASH_SIZE, i, BLAKE2S_KEY_SIZE);
+		blake2s(key, BLAKE2S_KEY_SIZE, buf, i, hash, BLAKE2S_HASH_SIZE);
 		if (memcmp(hash, blake2s_keyed_testvecs[i], BLAKE2S_HASH_SIZE)) {
 			pr_err("blake2s keyed self-test %zu: FAIL\n", i + 1);
 			success = false;
@@ -2080,7 +2080,7 @@
 	}
 
 	for (i = 0; i < ARRAY_SIZE(blake2s_testvecs); ++i) {
-		blake2s(hash, buf, NULL, BLAKE2S_HASH_SIZE, i, 0);
+		blake2s(NULL, 0, buf, i, hash, BLAKE2S_HASH_SIZE);
 		if (memcmp(hash, blake2s_testvecs[i], BLAKE2S_HASH_SIZE)) {
 			pr_err("blake2s unkeyed self-test %zu: FAIL\n", i + i);
 			success = false;
--- a/src/noise.c
+++ b/src/noise.c
@@ -36,10 +36,10 @@
 
 void __init wg_noise_init(void)
 {
-	struct blake2s_state blake;
+	struct blake2s_ctx blake;
 
-	blake2s(handshake_init_chaining_key, handshake_name, NULL,
-		NOISE_HASH_LEN, sizeof(handshake_name), 0);
+	blake2s(NULL, 0,handshake_name, sizeof(handshake_name),
+		handshake_init_chaining_key, NOISE_HASH_LEN);
 	blake2s_init(&blake, NOISE_HASH_LEN);
 	blake2s_update(&blake, handshake_init_chaining_key, NOISE_HASH_LEN);
 	blake2s_update(&blake, identifier_name, sizeof(identifier_name));
@@ -307,7 +307,7 @@
 
 static void hmac(u8 *out, const u8 *in, const u8 *key, const size_t inlen, const size_t keylen)
 {
-	struct blake2s_state state;
+	struct blake2s_ctx state;
 	u8 x_key[BLAKE2S_BLOCK_SIZE] __aligned(__alignof__(u32)) = { 0 };
 	u8 i_hash[BLAKE2S_HASH_SIZE] __aligned(__alignof__(u32));
 	int i;
@@ -434,7 +434,7 @@
 
 static void mix_hash(u8 hash[NOISE_HASH_LEN], const u8 *src, size_t src_len)
 {
-	struct blake2s_state blake;
+	struct blake2s_ctx blake;
 
 	blake2s_init(&blake, NOISE_HASH_LEN);
 	blake2s_update(&blake, hash, NOISE_HASH_LEN);

